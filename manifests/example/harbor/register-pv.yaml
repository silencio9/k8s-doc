apiVersion: v1
kind: PersistentVolume
metadata:
  name: harbor-registry
  labels:
    name: harbor-registry
spec:
  local:
    path: /data/harbor/registry
    fsType: xfs
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k8s01.example.yaml
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 50Gi
